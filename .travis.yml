sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t dante01yoon/docker-react-app Dockerfile.dev .

script:
  - docker run -e CI=true dante01yoon/docker-react-app npm run test -- --coverage

after_success:
  - echo "Test Success"